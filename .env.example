NODE_ENV=development
PORT=3000

# Redis (local docker)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# Mongo (local docker)
MONGO_URI=mongodb://mongo:27017/rate_limiter_db

# CORS origin - set to frontend's origin in production
CORS_ORIGIN=*

# Default limits (we'll load these later from config / admin APIs)
DEFAULT_POINTS=100
DEFAULT_WINDOW_SECONDS=60
